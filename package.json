{
  "name": "reservoir-dogs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx lerna run start '--ignore=@reservoir-dogs/*(functions|showcase)' --stream --parallel",
    "build": "npx lerna run build '--ignore=@reservoir-dogs/*(ner|summarization)' --stream",
    "build:docker": "npx lerna run build '--scope=@reservoir-dogs/*(ner|summarization)' --stream",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx lerna run lint",
    "deploy-ner": "gcloud app deploy packages/ner/app.yaml"
  },
  "author": "",
  "private": true,
  "devDependencies": {
    "husky": "^3.0.5",
    "lerna": "^3.16.4",
    "prettier": "^1.18.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn run lint"
    }
  },
  "workspaces": [
    "packages/*"
  ]
}
