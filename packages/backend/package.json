{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build:live",
    "build": "npx tsc ./index.ts --outDir './lib'",
    "build:live": "NODE_ENV=DEVELOPMENT nodemon --watch './**/*.ts' --exec 'ts-node' ./index.ts"
  },
  "dependencies": {
    "node": "^12.7.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "@types/node": "^12.7.5"
  },
  "author": "",
  "license": "ISC"
}
